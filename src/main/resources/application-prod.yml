spring:
  application:
    name: user-center
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/user_center_serve
    # session 失效时间
  session:
    timeout: 86400 # 一天，单位秒
